{
  "version": 2,
  "builds": [
    {
      "src": "api/webhook.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/webhook",
      "dest": "api/webhook.ts"
    }
  ],
  "env": {
    "BOT_TOKEN": "@number_lookup_bot_token",
    "ADMIN_CHAT_ID": "@number_lookup_admin_id",
    "UPI_ID": "@number_lookup_upi_id",
    "API_KEY": "subharansu-200"
  }
}